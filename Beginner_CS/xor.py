from operator import xor


def myXOR(p, q):
    # 関数を完成させてください
    if p != q:
        return True
    else:
        return False


print(xor(True, False))

# myXOR(True, True)
# myXOR(True, False)
# myXOR(False, True)
# myXOR(False, False)
